# Agent-local env (optional). You can keep keys here OR in the repo-root .env —
# the agent loads both. See ../.env.example for the full documented list.
GEMINI_API_KEY=
LINKUP_API_KEY=
# OFFLINE=1
# MODEL=gemini-3.5-flash
